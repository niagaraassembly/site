# Raw API responses. Matches at any depth -- an earlier `scripts/.cache/`
# pattern missed atlas/us/scripts/.cache/ entirely and would have committed
# it.
.cache/

# Python virtualenv for the ingestion/enrichment pipeline
.venv/

# Build-time Node tooling (mapshaper)
node_modules/
