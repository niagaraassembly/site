name: Site request maintenance

on:
  workflow_dispatch:

permissions:
  contents: write
  issues: write

jobs:
  noop:
    name: No automatic CRM event handling
    runs-on: ubuntu-latest
    steps:
      - run: echo "Board approval events are handled by niagaraassembly/crm."
